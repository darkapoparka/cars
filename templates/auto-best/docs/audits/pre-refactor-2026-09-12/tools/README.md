# Superseded one-off tooling

Historical captures, corrective patches and export experiments. Their old absolute paths, UI variants and selectors are not active tests or migration instructions. Do not replay them. Current gates are in package.json and docs/TESTING.md.
