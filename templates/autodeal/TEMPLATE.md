# AutoDeal Full

Key: `autodeal` · version: `2026.09.06-baseline` · family: `autodeal`.

Complete existing Agency OS library, 150 stored page routes and ten real homepage compositions. Different runtime from auto-best. Scores are family-level visual judgement, not an endorsement of every home.

## Status

reference-content-candidate. Source branding/demo content is retained as a visual baseline. This is a candidate for personalization, not a finished generic config-driven template or a sendable lead demo.

## Provenance

M:\codex\agency-os-templates\verticals\automotive\templates\autodeal; 392bb1b5f26596582ea6fa7e547913eb3f8b86b7

Existing license/asset notes remain with the source. No new multi-client rights determination was made. Source instructions and old project task ledgers are historical; J:/cars/AGENTS.md governs this copy.

## Run

Node >=20; the server has no package dependencies. Run `node server.mjs` with an explicit PORT, or use the Cars helper.

From J:/cars, on a free port:

```powershell
./scripts/start-preview.ps1 -Template autodeal -Port 6460
```

Suggested library URL: http://127.0.0.1:6460/. The suggestion is not proof that a listener is running; see the audit runtime record. Original inspected source port: none; inspected the new 6460 library runtime.

`npm run build` verifies mirrored resources, i18n and syntax.

## Real homepage choices

- `home01`: `/`
- `home02`: `/home02`
- `home03`: `/home03`
- `home04`: `/home04`
- `home05`: `/home05`
- `home06`: `/home06`
- `home07`: `/home07`
- `home08`: `/home08`
- `home09`: `/home09`
- `home10`: `/home10`

Copy the whole project to retain all variants. Select a primary entry after copying; retain alternate-home choice links when requested.

## Personalization boundaries

- `mirror/pages`
- `mirror/public/assets`
- `i18n/site-config.mjs`
- `i18n/catalogs`
- `server.mjs`

These are current code/data ownership locations, not a promise that one config edits the whole app. Scan every retained route, metadata, contact value and identity-bearing asset after changes.

This is the full existing Node/static AutoDeal library, not the newer native auto-best. It retains 150 stored routes including locales/variants, and 479 local resources. All ten home layouts render locally. Some Bulgarian text is partial/mixed and source vehicle labels do not match their imagery. These are mandatory content corrections in a client skin.

## Representative QA routes

- `/`
- `/home02`
- `/home03`
- `/home04`
- `/home05`
- `/home06`
- `/home07`
- `/home08`
- `/home09`
- `/home10`
- `/listing-grid`
- `/listing-detail-v1/1`
- `/contact`

Use 390 and 1440 px, plus every offered home. Exercise navigation, filters, detail return, overlay dismissal and the main contact path. Existing source data and frontend feedback do not prove real form delivery or a working provider integration.
