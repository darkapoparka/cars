name: Apply approved dealer logo refresh

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Automatic promotion is disabled; use the artifact-only preparation and reviewed promotion flow."
