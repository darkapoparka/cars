import fs from 'node:fs/promises';const root='J:/cars/clients/ivo-auto';for(const file of ['qa-routes.mjs','qa-auto-interactions.mjs']){let s=await fs.readFile(root+'/'+file,'utf8');s=s.replace('for(const width of [390,1440])','try { for(const width of [390,1440])').replace('await b.close();await fs.writeFile','} finally { await b.close(); } await fs.writeFile');await fs.writeFile(root+'/'+file,s)}
