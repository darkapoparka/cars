# Любо кар review

State: implemented-unverified on `codex/astra-bg-08`.

- Auto Best: `cd clients/lyubo-car/auto-best`; `npm ci`; `npm run validate`; entry `/`.
- Modern: `cd clients/lyubo-car/modern`; use retained Node/pnpm; `pnpm install --frozen-lockfile`; build database package for local Prisma generation, then typecheck/build `apps/web`; entry `/cars`.
- Carwow: `cd clients/lyubo-car/carwow`; `npm ci`; `npm run check`; `npm run build`; entry `/`.

Inspect entry, inventory, one sample detail, contact, navigation, filter/reset and demo enquiry destination at 390 and 1440 px; header/logo controls at 320 px. Do not submit external enquiries. The same eight dated seller-advertised records drive all three variants. Local neutral image placeholders are not vehicle photos and may only be replaced with matched permitted listing media.
