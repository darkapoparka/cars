import {featuredVehicles} from './inventory';import {bodyLabel} from './listing';export const bodyTypes=[...new Set(featuredVehicles.map(v=>v.body))].map(query=>{const s=featuredVehicles.find(v=>v.body===query)??featuredVehicles[0];return{label:bodyLabel(query),query,image:s.image,width:180,height:108,count:featuredVehicles.filter(v=>v.body===query).length}});export const brands=[{label:'Porsche',image:featuredVehicles[0].image,count:featuredVehicles.length}];export const editorial=[{title:'Confirm current stock',text:'The catalogue changes; confirm the exact car before travelling.',image:featuredVehicles[0].image,href:'/contact',meta:'Showroom guide',category:'Viewing'},{title:'Compare dated listings',text:'Use published price, year and kilometres as a dated snapshot.',image:featuredVehicles[1].image,href:'/listing-grid',meta:'Stock',category:'Buying'},{title:'Import and export enquiries',text:'Ask the showroom about current vehicle-specific import or export terms.',image:featuredVehicles[2].image,href:'/contact?topic=import',meta:'Service',category:'Showroom'}] as const;
