> Consolidated on 9 September 2026: active source is now `J:/cars/clients/kg-team-auto` in the main Cars repository on `main`. Old session paths and publish-to-astra instructions below are historical. Do not recreate those workspaces. Application completion and owner visual review remain pending; see [the current project inventory](../../docs/PROJECTS.md).

# K-G Team Auto — session 02 implementation

Status: implementation in progress. These are actual independent applications copied from the assigned masters, not a generator or a completed launch.

## Applications

- `auto-best/`: SvelteKit master `2026.09.08-polish-1`; home `/`.
- `modern/`: complete Next.js workspace master `2026.09.06-refresh-1`; public app `apps/web`, home `/bg/cars`.
- `carwow/`: SvelteKit master `2026.09.08-repair-1`; home `/`.

The imported inventory boundaries in all three now use the same ten seller-advertised samples captured on 2026-09-09. Source prices are EUR and distances are kilometres. Availability remains unconfirmed after capture. `inventory-source.json` retains listing IDs, source URLs and image attribution. `business-facts.json` separates published facts from unknown services and hours.

Published TEAM AUTO artwork is used for the K-G Team Auto profile at `team-auto.mobile.bg`. Original artwork, optimized light/dark versions, a source-letter favicon, local listing galleries and source-photo hero cutouts are integrated. No generated or generic replacement text logo is presented as official branding.

## Current verification

Auto Best framework check completed with zero errors and seventeen warnings after the first personalization pass. Other checks and browser verification are still in progress. No production build, full-route acceptance or visual readiness is claimed by this checkpoint.

The source templates and existing clients have not been edited. The original recovery checkout was preserved when another local process changed its branch. Implementation continues in a separate worktree on `I:` after the first dependency install encountered a space error on `J:`.

## Not approved for launch

This is an unapproved prospect concept, not an official dealer website. Public visibility of dealer images is not a licence grant; permission for public reuse is not evidenced. No deployment, dealer contact, account creation or private sales-record change has been performed. Source-based image rights, final content verification and remaining application checks must be resolved before launch. Forms must remain non-delivering in preview.
