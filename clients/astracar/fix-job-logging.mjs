import fs from 'node:fs/promises';const root='J:/cars/clients/astracar';for(const name of ['install-auto','install-modern','install-carwow','check-auto','check-modern','check-carwow']){let s=await fs.readFile(root+'/'+name+'.ps1','utf8');s=s.replace(/^(npm\.cmd|pnpm\.cmd) (.*?) \*> (.*?)$/gm,(_,exe,args,log)=>`cmd /c "${exe} ${args} > ${log} 2>&1"`);if(name.includes('carwow'))s=s.replace("$env:PATH='C:/Users/radev/AppData/Local/nvm/v22.23.2;'+$env:PATH","# Carwow retains its Node 24 requirement; use the host Node 24 runtime.");await fs.writeFile(root+'/'+name+'.ps1',s);}
