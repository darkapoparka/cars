# Navara Car / Modern

Independent client preview copied from the actual templates/modern master. Keep the complete apps/packages workspace, the retained lockfile and existing storefront layouts. Read the repository-root AGENTS.md, docs/WORKFLOW.md, ../HANDOFF.md and .client/project.json. Do not resume source-dealer tasks, deploy, send messages, register CRM records or change other clients. Never inherit source QA results. Public preview uses the existing static demo mode; do not provision a database, authentication or delivery integration.
