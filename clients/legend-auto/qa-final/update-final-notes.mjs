import fs from 'node:fs/promises';const p='J:/cars/clients/legend-auto/qa-final/final-records.mjs';let s=await fs.readFile(p,'utf8');s=s.replace("evidence:key==='auto-best'?", "evidence:key==='auto-best'&&slug!=='legend-auto'?");s=s.replace('LEGEND Modern directory now contains its real unverified dealer and captured vehicles instead of missing synthetic inventory.', 'LEGEND Modern directory now contains its real unverified dealer and captured vehicles instead of missing synthetic inventory; feature values retain original text in the required bilingual shape and duplicate city keys were removed. LEGEND Carwow disclaimer CSS was merged into its existing style block. Legacy public proposal HTML was preserved in provenance and replaced by home/contact redirects; Automarket/ELIT build logs predate this static-only correction.');await fs.writeFile(p,s);
