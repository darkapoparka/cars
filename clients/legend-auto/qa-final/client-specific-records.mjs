import fs from'node:fs/promises';const p='J:/cars/clients/legend-auto/qa-final/final-records.mjs';let s=await fs.readFile(p,'utf8');s=s.replace("await fs.writeFile(root+'/qa-final/FINAL.md',final);", "if(slug!=='legend-auto')final=final.replace(/ LEGEND Modern directory[\\s\\S]*?LEGEND Carwow disclaimer CSS was merged into its existing style block\\./,'').replace('Auto Best framework validation is reused because its source is unchanged; browser evidence is from 2026-09-08, including a fresh LEGEND run on safe port 6671.','Auto Best framework validation and 2026-09-08 browser evidence are reused because its source is unchanged.');\nfinal=final.replace('Automarket/ELIT build logs predate this static-only correction.',slug==='legend-auto'?'Proposal redirects were included in the retry build; the manifest correction followed it.':'The Carwow build log predates this static-only correction.');\nawait fs.writeFile(root+'/qa-final/FINAL.md',final);");await fs.writeFile(p,s);
