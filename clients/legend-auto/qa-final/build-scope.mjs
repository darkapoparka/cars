import fs from'node:fs/promises';const p='J:/cars/clients/legend-auto/qa-final/final-records.mjs';let s=await fs.readFile(p,'utf8');s=s.replace("frameworkChecks:build,checkedAt", "frameworkChecks:build,...(key==='carwow'?{buildScope:'Build passed before final static and contact-copy corrections; final Svelte check and targeted browser proof are recorded separately.'}:{}),checkedAt");await fs.writeFile(p,s);
