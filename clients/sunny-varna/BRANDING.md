# СЪНИ — source-identity integration checkpoint

2026-09-09. Branch: `codex/astra-bg-03`. Parent checkpoint: `d853ae36428f250821661a8f3b5ab0b4cb802930`.

The published banner was recovered and opened as an image. It shows custom black СЪНИ lettering on lime green, not the previous proposed sun/road emblem. All three applications now contain the preserved source PNG and source-matched vector/raster exports at their existing `/brand/logo.svg`, `/brand/logo-dark.svg`, `/brand/logo.png` and icon paths. The same high-contrast plate is intentional on light and dark surfaces. No master, other client, shared index or deployment binding was changed.

Carwow also receives real local bytes at its retained `/brand/daynight-logo-generated.png` and `/brand/daynight-og.svg` compatibility paths. These filenames are legacy internal references; the assets show СЪНИ. This repairs missing branding paths without changing component layout, route composition or startup behavior.

The three brand manifests use the published lime identity; Modern starts at `/cars`, Auto Best and Carwow at `/`. The SVG social card is 1200 × 630; it is not a raster PNG, and social-platform acceptance has not been tested.

See each application's `brand/PROVENANCE.md` for source URL, creation method, exact asset hashes and limits. Source visibility does not establish a reuse license or dealership approval. No successful ImageGen result is claimed for this source-based cleanup.

Executed: PNG decode; SVG raster rendering and visual inspection of lettering/margins; local Git-blob hash comparisons against the uploaded blob responses. These checks are not full application renders, responsive QA, installs, framework builds or owner approval.

Still in progress: the remaining inherited-content sweep, correctly sourced and permitted real vehicle galleries, application builds, and route/interaction review at 320/390/1440 pixels. The existing application QA flags remain false. This checkpoint does not label any of the fifteen applications complete.
