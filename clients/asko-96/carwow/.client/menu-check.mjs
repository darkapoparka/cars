import {chromium} from '@playwright/test';const b=await chromium.launch({headless:true});const p=await b.newPage({viewport:{width:390,height:844}});for(const port of [6613,6517]){await p.goto(`http://127.0.0.1:${port}/inventory`,{waitUntil:'domcontentloaded'});await p.waitForTimeout(1500);await p.getByRole('button',{name:'Меню',exact:true}).click();await p.getByRole('dialog',{name:'Меню',exact:true}).getByRole('button',{name:'Затвори',exact:true}).click();await p.waitForTimeout(1600);console.log(port,await p.locator('[role=dialog]').evaluateAll(es=>es.map(e=>({state:e.getAttribute('data-state'),aria:e.getAttribute('aria-hidden'),inert:e.inert,rect:e.getBoundingClientRect().toJSON(),display:getComputedStyle(e).display}))),await p.getByRole('button',{name:'Меню',exact:true}).getAttribute('aria-expanded'));}await b.close();
