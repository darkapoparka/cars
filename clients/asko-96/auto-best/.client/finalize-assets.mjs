import fs from 'node:fs';import path from 'node:path';
const videos=[{id:'MZryizpxa6k',title:'Porsche Cayenne Може Всичко',duration:'5:24'},{id:'DoIn6yzLoyY',title:'Този Mercedes S500 има ВСИЧКО',duration:'5:24'},{id:'rgcZmHqkdWw',title:'Mercedes GLC 220 - Голям Багажник и Малък Разход',duration:'4:06'}];
for(const v of videos){v.thumbnail='/assets/asko96/video-'+v.id+'.jpg';const r=await fetch('https://i.ytimg.com/vi/'+v.id+'/hqdefault.jpg');if(!r.ok)throw Error(r.status);fs.writeFileSync('static'+v.thumbnail,Buffer.from(await r.arrayBuffer()));}
fs.writeFileSync('src/lib/data/videos.ts','export interface FeaturedVideo { id: string; title: string; duration: string; thumbnail: string; }\n// Curated official ASKO96 channel videos, verified 2026-09-06.\nexport const featuredVideos: readonly FeaturedVideo[] = '+JSON.stringify(videos,null,2)+';\n');
const walk=dir=>fs.readdirSync(dir,{withFileTypes:true}).flatMap(x=>x.isDirectory()?walk(path.join(dir,x.name)):[path.join(dir,x.name)]);
for(const file of walk('src'))if(/\.(svelte|css)$/.test(file)){let s=fs.readFileSync(file,'utf8').replaceAll('#c40101','var(--dn-red)').replace(/rgba\(196,\s*1,\s*1,/g,'rgba(215, 174, 53,');s=s.replace(/([^{}]+)\{([^{}]+)\}/g,(whole,sel,body)=>{if(/background(?:-color)?:\s*var\(--dn-red(?:-hover)?\)/.test(body))body=body.replace(/color:\s*(?:#fff(?:fff)?|white);/g,'color: var(--dn-accent-ink);');body=body.replace(/(?<![-\w])color:\s*var\(--dn-red\)/g,'color: var(--dn-accent-text)');return sel+'{'+body+'}';});fs.writeFileSync(file,s);}
