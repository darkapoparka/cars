import fs from 'node:fs/promises';const root='J:/cars/clients/avangard-auto/carwow/src/lib/client/';for(const name of ['lead-submit.ts','import-request-submit.ts']){let s=await fs.readFile(root+name,'utf8');s=s.replace("import { resolve } from '$app/paths';",'');const start=s.indexOf('function isJsonObject');const fn=s.slice(s.indexOf('export async function'),s.indexOf('\n return { ok: false'));s=s.slice(0,start)+fn+'\n return { ok: false, status: 503, error: "Демонстрационна форма. Обадете се на 0877 800 921." };\n}\n';await fs.writeFile(root+name,s);}
