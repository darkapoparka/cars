import fs from 'node:fs/promises';import {chromium} from '../../templates/boxcar/node_modules/playwright/index.mjs';const b=await chromium.launch({channel:'chrome',headless:true});const p=await b.newPage();for(const [name,url] of [['facebook','https://www.facebook.com/avangardautovarna/'],['instagram','https://www.instagram.com/avangard_auto_varna/']]){try{await p.goto(url,{waitUntil:'domcontentloaded',timeout:20000});const d=await p.evaluate(()=>({url:location.href,title:document.title,text:document.body.innerText.slice(0,10000),images:[...document.images].map(i=>({src:i.src,alt:i.alt})),og:[...document.querySelectorAll('meta[property]')].map(m=>({property:m.getAttribute('property'),content:m.content}))}));await fs.writeFile('clients/avangard-auto/evidence/'+name+'.json',JSON.stringify(d,null,2));console.log(name,JSON.stringify(d).slice(0,1800));}catch(e){console.log(name,e.message)}}await b.close();
