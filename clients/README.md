# Client and prospect work

This folder holds potential clients as well as won clients. Its name does not indicate commercial status.

`dayandnight`, `eliqauto` and `asko96` currently contain briefs only. Existing demos on M: remain in their original folders. New personalized copies belong at `<client>/<template-key>/` when requested.

Use `../docs/WORKFLOW.md` and `../scripts/new-client.mjs`. Never modify a shared template to personalize one lead. The helper records template lineage and refuses to overwrite a project.
