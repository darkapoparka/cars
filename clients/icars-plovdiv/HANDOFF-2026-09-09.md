# icars — three-design implementation

State: implemented-unverified. Source master commit: faf76e81c96a4e6dbe18e8ffcaf0a249df47b7ca. Branch: codex/astra-bg-05.

## Entries and retained runtimes

- auto-best: /; Node 22.12+ in Node 22; npm ci; npm run validate.
- modern: /cars; retain the full workspace. Node >=22.22 <23; pnpm 11.4.0; pnpm install --frozen-lockfile; pnpm --filter @repo/database build (local Prisma generation only); pnpm --filter web typecheck.
- carwow: /; Node 24; npm ci; npm run check; npm run build.

Run commands from the corresponding application/workspace, not from the Cars repository root. Do not run a Next build against a dev server's output directory. Suggested review ports 6631/6632/6633 have not been inspected or started by this branch session. The coordinator may use the existing J:/cars launcher after preserving local edits and integrating the finished branch. No local checkout was changed here.

## Content and media

10 individually read public listings, with six original locally bundled photos each, consistent source IDs, EUR prices and native kilometre values across all variants. Source facts and hashes are in FACTS-AND-INVENTORY.json. No unavailable provider has been connected, and no enquiry or credit approval is represented as delivered. Publication and manual acceptance remain pending.

## Verification

Current checkpoint is source implementation, not application runtime approval. Source compilation/analysis is performed in memory using TypeScript 5.9.3 and the retained Svelte compiler; this is not npm/pnpm installation, web typecheck, production build or browser review. Exact final source checks and remaining content items are recorded in the session report.

Final source-only evidence: SOURCE-QA.json. Exact review entries: REVIEW.md. Previous implementation checkpoint(s): .
