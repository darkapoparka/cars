import{brand}from'$config/brand';import{featuredVehicles}from'./inventory';export type BlogCategory='Buying'|'Viewing'|'Stock';export type BlogPost={id:number;title:string;text:string;category:BlogCategory;tag:string;image:string;sections:Array<{title:string;paragraphs:string[]}>};export type BlogFilters={q:string;category:BlogCategory|''};export const blogPosts:BlogPost[]=[{id:1,title:'Confirm the exact vehicle before travelling',text:'Rare and high-value collection stock can change quickly.',category:'Stock',tag:brand.city,image:featuredVehicles[0].image,sections:[{title:'Use the listing as context',paragraphs:['Share the exact vehicle listing and confirm current availability and price directly with F1rst Motors.']}]},{id:2,title:'Prepare for a premium showroom viewing',text:'Keep the visit focused on the exact car and questions that matter.',category:'Viewing',tag:brand.city,image:featuredVehicles[1].image,sections:[{title:'Before the visit',paragraphs:['Confirm the vehicle and appointment, then prepare questions about condition, provenance and specifications.']}]},{id:3,title:'Compare rare cars carefully',text:'Price, kilometres, specification and provenance all matter.',category:'Buying',tag:'Guide',image:featuredVehicles[2].image,sections:[{title:'Compare the full picture',paragraphs:['Treat this concept as dated source information and confirm all commercial and technical details directly.']}]}];export const blogCategories:BlogCategory[]=['Buying','Viewing','Stock'];const isCat=(v:string|null):v is BlogCategory=>Boolean(v&&blogCategories.includes(v as BlogCategory));export const parseBlogFilters=(p:URLSearchParams):BlogFilters=>({q:p.get('q')?.trim()??'',category:isCat(p.get('category'))?p.get('category') as BlogCategory:''});export const filterBlogPosts=(posts:BlogPost[],f:BlogFilters)=>posts.filter(p=>(!f.category||p.category===f.category)&&(!f.q||`${p.title} ${p.text} ${p.tag}`.toLowerCase().includes(f.q.toLowerCase())));export const blogFilterHref=(f:BlogFilters,c:BlogCategory|'')=>{const p=new URLSearchParams();if(f.q)p.set('q',f.q);if(c)p.set('category',c);const q=p.toString();return q?`/blog?${q}`:'/blog'};
