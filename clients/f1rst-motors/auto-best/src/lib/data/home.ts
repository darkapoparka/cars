import{featuredVehicles}from'./inventory';import{bodyLabel}from'./listing';export const bodyTypes=[...new Set(featuredVehicles.map(v=>v.body))].map(query=>{const s=featuredVehicles.find(v=>v.body===query)??featuredVehicles[0];return{label:bodyLabel(query),query,image:s.image,width:180,height:108,count:featuredVehicles.filter(v=>v.body===query).length}});export const brands=[...new Set(featuredVehicles.map(v=>v.make))].map(label=>{const s=featuredVehicles.find(v=>v.make===label)??featuredVehicles[0];return{label,image:s.image,count:featuredVehicles.filter(v=>v.make===label).length}});export const editorial=[{title:'Confirm the vehicle before your visit',text:'Rare and high-value stock changes. Confirm current availability directly.',image:featuredVehicles[0].image,href:'/contact',meta:'Showroom guide',category:'Viewing'},{title:'Compare the collection clearly',text:'Use the dated source price, year and kilometres as a starting point.',image:featuredVehicles[1].image,href:'/listing-grid',meta:'Collection',category:'Buying'},{title:'Sell-my-car enquiry',text:'The official site provides a direct vehicle-sale enquiry journey.',image:featuredVehicles[2].image,href:'/contact?topic=trade-in',meta:'Service',category:'Showroom'}] as const;
