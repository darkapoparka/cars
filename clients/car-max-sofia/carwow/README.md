# CAR MAX / Carwow — in progress

Independent source copied from `templates/carwow` at `faf76e81c96a4e6dbe18e8ffcaf0a249df47b7ca`, version `2026.09.08-repair-1`. Carwow is the template's internal nickname, not an affiliation. Personalization, media and acceptance are not complete. Do not deploy or send this checkpoint.

Use the retained Node 24 runtime and lockfile: `npm ci`, `npm run check`, `npm run build`. For a local preview, after checking the port is free: `npx vite --host 127.0.0.1 --port 6633 --strictPort`. Do not use the inherited fixed-port dev wrapper. Entry `/`, inventory `/inventory`, contact `/contact`.

No install, checks, build or browser review has run for this copy. Source TEMPLATE.md, architecture and provenance documents describe the master, not CAR MAX approval. The historical `.template-ref` snapshot, old template metadata and design QA report are excluded from this independent copy.
