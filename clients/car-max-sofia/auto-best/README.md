# CAR MAX / Auto Best — in progress

Independent source copied from `templates/auto-best` at `faf76e81c96a4e6dbe18e8ffcaf0a249df47b7ca`, version `2026.09.08-polish-1`. This is application source, not a finished personalization. Source-business content/media still need the client sweep. Do not deploy or send it.

Node 22.12+ in the Node 22 line. Install with `npm ci`; run `npm run validate`. From this directory use `npx vite --host 127.0.0.1 --port 6631 --strictPort` only after confirming the port is free. Entry `/`, inventory `/listing-grid`, contact `/contact`.

No install, typecheck, build or browser check has run for this copy. See `../SOURCE-REVIEW.json` and `.client/project.json`. Historical TEMPLATE, license and provenance notes describe the master, not CAR MAX acceptance.
