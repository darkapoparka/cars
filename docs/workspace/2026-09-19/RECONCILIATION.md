# Local main reconciliation — 19 September 2026

Verified at 2026-09-19T14:29:01.397Z.

J:/cars was advanced in place from cd526d2181ab878f1abd83a67a640c817379714d to GitHub main df22295b9649fdd6aaf6ad453113e99b7c93acdf, preserving main and its existing worktree. At the verification point HEAD and origin/main matched, with zero commits ahead or behind and no staged files. This receipt is not a claim that unrelated drafts are committed or accepted.

## Preservation and source reconciliation

The initial audit preserved 5,523 existing modified/untracked files (499,065,414 bytes) and recorded 156 local deletions. Twelve additional unchanged text files with CRLF-only differences were also preserved after Git required explicit refresh. Recovery evidence is under ignored J:/cars/runtime/workspace-reconcile-20260919T1410, including original index, per-file SHA-256 records, original files and synchronization journal. No backup was uploaded as new application source.

914 reviewed tracked paths now match the selected committed source, including the currently delivered dealer sources and workspace helpers. 263 untracked paths that became tracked upstream were preserved separately before the upstream copies were installed. Other untracked drafts remain in place. No generated logo was recreated or restyled.

520 unrelated tracked draft paths were verified byte-for-byte unchanged: 509 for Exclusive Auto Varna, ten for Tesi Car Varna and leads/README.md. The Bulgaria research JSON was reconciled by stable lead ID and a strict three-way merge: 17 base records, 32 local records and 18 remote records produced 32 retained records with no unresolved field conflicts. This research remains an unstaged draft rather than being silently included in the documentation commit.

Two guarded fast-forward attempts aborted before the final successful attempt. The original working files were restored from the verified copies between attempts. Twelve CRLF-only files were omitted by Git's normalized name-only diff but still blocked its checkout; their normalized bytes were verified equal to the old commit, preserved and included in the explicit refresh. No reset, clean, force-push or global Git-setting change was used.

## Logos and hosted sites

The logo agent's committed [delivery report](../../dealer-finalization/2026-09-19/ADMIN-LOGO-VERIFICATION-1304.md) records 216 logo-file matches and 144 rendered checks across the 24 live dealers. This task synchronized that committed work; it did not regenerate logos or claim additional owner visual approval. No Vercel deployment, domain, protection, retention or billing setting was changed by this reconciliation.

## Remaining boundaries

The six canonical repositories retain the physical locations in [WORKSPACE](../../WORKSPACE.md). Existing C:/Users/radev review/recovery/build folders were not bulk-moved or deleted. The [folder inventory](FOLDERS.json) remains a preservation queue, not a claim of completed physical cleanup or reclaimed disk space. New generated work stays under runtime.

Other repository drafts were not published or overwritten: Modern was ahead of its remote; Import had active edits; Carwow was behind with local edits; Cars Admin had divergent local and remote commits; Auto Best's fetch reported a broken local Codex checkpoint ref. Those are separate repository findings, not proof that their local draft is the approved deployed template. Their active applications and processes were left running.

The next hosting direction is [shared source with hybrid delivery](../../PREVIEW-ARCHITECTURE.md). A shared multi-tenant platform is still a pilot proposal, not a completed migration.

## Verification of this maintenance change

The full Node workflow suite was run against the synchronized checkout. Its first run reported 46/51 passing: five legacy refresh fixtures still expected superseded logo filenames or omitted the new contract assets/consumer files and Import menu header. The fixtures now include those required inputs, assert the current approved contextual paths, and verify all output logo hashes against their saved contracts. Existing hero, identity and inventory assertions remain. No application or logo implementation was altered to make the tests pass.

After that fixture repair, **51/51 tests passed**, including the previously data-dependent refresh integration tests. The active documentation/skill/command check passed, and both new documents passed relative-link checks. No production browser suite was rerun by this maintenance change; the independent logo agent report remains the dated hosted evidence.

## Subsequent logo commits and organization

The [organization completion receipt](ORGANIZATION.md) records the later fast-forward through dac319e1, preservation verification, image-evidence consolidation and the final 52/52 workflow tests. This earlier receipt remains dated evidence of the initial reconciliation.
