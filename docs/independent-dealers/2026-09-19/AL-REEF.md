# Al Reef Used Cars — independent dealer workflow proof

19 September 2026. The existing interrupted pilot was completed, not cloned into another source folder.

## Delivered project

Public URL: https://cars-alreefusedcars.vercel.app/

Sole editable source: https://github.com/darkapoparka/cars-alreefusedcars (private), main, at I:/cars-clients/al-reef-used-cars. The dealer repository contains Auto Best, Modern and Carwow, with entries /, /variant-2/cars and /variant-3/. The floating chooser also opens the shared synthetic Cars Admin demo. No dealer application tree was added under Cars/clients.

Application commit: 68bdd79458f233b0af9327fe7f73336866f4408a. Evidence-only commit: 8fd44decc13d3ef2033180f3ddc68dc4fd6b9187. The Git-linked initial production deployment is dpl_CUn9wywk647fvkwMdPpMFYd8zWRt in project prj_r5Vaw6NQh5iTxi5jjfrtuP9GFTNm. The registry records current provider observations separately from this dated initial delivery.

[Dealer handoff](https://github.com/darkapoparka/cars-alreefusedcars/blob/main/docs/HANDOFF.md) and [delivery evidence](https://github.com/darkapoparka/cars-alreefusedcars/blob/main/docs/DELIVERY.json) contain the exact public browser checks and screenshots.

## What was built and checked

The official logo was retained as transparent contextual PNG/WebP assets; no new generated identity was needed. English primary journeys and AED presentation use public Sharjah business facts. Six cars and prices are explicitly illustrative, not verified Al Reef availability. No real enquiries, bookings, database, live AI or private agency CRM are connected.

All three local production builds passed. Modern typecheck passed; Carwow check/build passed after a non-modal drawer focus repair. The 42-check local and 42-check public browser matrices passed across 1440, 390 and 320 widths. All 33 public asset hashes matched committed bytes (15 logo/icon files and 18 illustrative vehicle images). Three synthetic write probes were rejected with 403 before any business action.

AGENTS.md defines content/branding defaults and permits deliberately requested client features. The build-time guard checks 33 asset hashes, 208 protected presentation/artwork files, 154 component style blocks, source pins, routes and demo safety. Six guard tests include negative tampering, layout, fourth-design, safety-removal and focus-trap cases. Instructions alone are not the enforcement mechanism; these checks run before every service build.

## Reusable workflow corrections

Cars gained independent registry/dashboard ownership support, so links point to the dealer repository instead of a nonexistent Cars source folder. The Modern mounting adapter uses the configured default locale instead of forcing Bulgarian. The shared FAB closes at the capture phase before unrelated page Escape handlers. The non-modal Carwow detail drawer explicitly disables focus trapping in this dealer; that small behavior fix is recorded for upstream review rather than represented as an already-released template change.

The existing 24 legacy dealers, template masters and their deployment aliases were not migrated or republished. Other local draft source was not staged. Future work on Al Reef continues in the same dealer repository and main checkout; the legacy Cars exporter must not overwrite it.

## Workflow verification

The final full Cars workflow suite passed 79/79 tests with no exclusions, including independent registry and dashboard-link regressions. The 12-document/three-skill workspace check passed. These tooling checks are separate from the dealer's six guard tests and 42 public browser checks.

The saved technical registry validates and its views were regenerated without rescanning old dealer drafts. A separate full local-manifest comparison reports an existing Exclusive Auto Varna draft repository-identity mismatch; it was not modified as part of this pilot and is not a failure of the new independent record.

## Final production identity

Git also built the evidence-only commit 8fd44decc13d3ef2033180f3ddc68dc4fd6b9187 as dpl_6nitkaAfDfACBPmfyyT4VX48EF3c. It is READY on the same public alias. A Git comparison confirmed no application, asset, script or deployment-configuration differences from the initial verified release, and 9/9 fresh entry/FAB checks passed on the final alias. The current registry points to this deployment. [Final proof](AL-REEF-DELIVERY.json) separates the original 42-check run from the final same-application smoke check. No further dealer-source push is needed to record this observation.
