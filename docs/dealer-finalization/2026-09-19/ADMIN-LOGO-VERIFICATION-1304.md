# Lead logos and Admin: verified production delivery

Verified 2026-09-19T13:33:52.553Z.

All 24 existing public lead projects now serve the pinned current template trios and committed contextual raster logos. The switcher keeps Design 1/2/3 and adds Admin dashboard, opening the shared public cars-admin demo in a new tab.

- 216/216 public logo files match the saved logo contracts.
- 144/144 rendered variant checks passed at 1440 and 390 pixels; count, three design links, Admin, Escape, images and overflow checked.
- Admin was actually opened from all 24 lead homepages; original tabs retained.
- Admin seven views passed at 1440, 390 and 320 pixels.
- PromoSale and OutletCars repaired Import releases additionally passed all three designs at 320 pixels before promotion.

## Corrections completed

The existing Import menu-key repair commits were released after repairing the Vercel ignored-build command. The former git diff command exited 129 in Services builds without a Git checkout. The tested 244-character rule skips only a known documentation-only diff; missing Git metadata and other uncertainty return 1 so Vercel builds. Applied to the 24 lead projects, without changing project roots, Git links, protection or retention settings.

Only PromoSale and OutletCars needed new deployments in this run. Current production origins and previous deployment rollback copies were retained. No billing changes, bulk deletion or local folder reorganization occurred.

The Admin dashboard is one shared client-facing demonstration, not a copy deployed per lead and not the private agency CRM. Its data is illustrative and browser-local. It does not edit the real dealer websites or connect their databases.

See ADMIN-LOGO-VERIFICATION-1304.json for exact Git commits, live deployment IDs, source pins and verification receipts. Owner visual approval is not asserted.
