# Exclusive Auto source worker

The publisher of bb04d80 continues only `clients/exclusive-auto-varna/` in this checkpoint. The other worker's `coordination/ACTIVE.md` and preserved patch JSON were read. NEXT CAR/FIVE AUTO and all other client trees are preserved, not overwritten. The compatible currency/km formatter was selectively reviewed from the other worker's preserved Carwow patch; its whole old snapshot was not applied.

This checkpoint integrates actual source/brand bytes, the three connected catalogue adapters, unavailable-review states, contact topics instead of fabricated staff, published contact links, explicit non-sending preview behavior and the strict client-specific Auto Best asset contract. Modern's eager directory import is changed from unrelated fixture IDs to this dealer's actual listings; general city translation dictionaries are restored rather than blindly replacing geography.

Before this checkpoint: 876 applicable source files compiled/transpiled/parsed without syntax errors. A read-only TypeScript program over the existing Modern dependencies found four client-level issues and one ImportMeta typing issue; fixes are included. The final repeat typecheck and real application build/browser verification are separately recorded when executed. Source compilation is not a full app build.

All requested code remains branch-only. No local checkout, server, database, deployment, dealer contact or CRM write. Original main/template base: faf76e81c96a4e6dbe18e8ffcaf0a249df47b7ca. A fresh-head non-force ref update must preserve concurrent sessions. The final CLIENT/REVIEW/metadata reconciliation follows validation of this code checkpoint; older statements that Carwow is still disconnected are now historical.
