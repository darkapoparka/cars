Repository: https://github.com/darkapoparka/cars
Target remote branch: astra
Batch: lead-build-2026-09-08 / Session 01 of 10

Implement my fixed allocation, not another plan. Build the following THREE candidate dealer sets, one dealer at a time; each set must have independent auto-best, full-workspace modern and carwow applications:

1. VALENTINO AUTO HOUSE — `bg-sofia-valentino-auto-house` — proposed `clients/valentino-auto-house`.
2. Success Automobile — `bg-plovdiv-success-automobile` — proposed `clients/success-automobile`.
3. VOIVODOV AUTO & ANTONIO — `bg-plovdiv-voivodov-auto-antonio` — proposed `clients/voivodov-auto-antonio`.

Read current AGENTS.md, docs/WORKFLOW.md, docs/LEAD-BUILD-GUARDRAILS.md, docs/lead-build/README.md, your entry in docs/lead-build/assignments.json, its source records/defaults, catalog.json and each master TEMPLATE.md through the connected GitHub tools before editing. Read your prior report first and resume your own partial work; do not regenerate it.

Bulgarian/Cyrillic branding; verify dealer domains rather than assuming marketplace-only status.

Check BOTH live main and astra for duplicate identities/aliases, profiles, domains and phones. Copy actual masters with the helper/equivalent exclusions. Preserve layouts/routes/interactions/dependencies. Prepare one current fact sheet, 8–12 sourced listing samples and a local permitted asset pack per dealer; wire them into all three actual apps. Visually inspect real branding. Use the real asset or an image-generation-assisted professional refresh with correct spelling, transparency, light/dark treatment and favicon; never finish with a generic Arial/CSS placeholder. Label generated artwork as a proposal, not an official original. Save/integrate/commit the assets, not just an image in chat.

Complete the entire identity/content sweep, market currency/units/language, safe demo forms, supported checks and per-dealer HANDOFF.md yourself. No inherited inventory, staff, reviews, finance promises, branding, passed QA or deployment bindings. No hotlink-only essential asset pack. Record genuine blockers without inventing facts or quietly replacing assigned dealers.

Publish actual application commits directly to astra using the fresh-head/non-force protocol in the execution contract. Reapply only your owned delta when another session advances the branch; never attach an old full tree to a new parent. Leave masters, main and unrelated client projects unchanged. Update only your assigned new client folders and `docs/lead-build/reports/session-01.json`. Do not deploy, contact anyone, connect real CRM/payments, create private sales records or archive anything.

Also inspect `clients/excellent-cars` (Excellent Cars) READ-ONLY for reuse: report the variants and current evidence actually found, without recreating or changing it. Do this after prioritizing the three new builds.

At the end account for all three candidate IDs and the reuse inspection. Give reachable astra SHA(s), app folder links, implemented changes, actual passed/not-run checks, limitations and a safe local-agent pull/install/start/inspect prompt. The local agent must not need to create/personalize the apps. Proceed without asking me to choose dealers or approve routine implementation details.
